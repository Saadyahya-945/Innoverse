'use client';

import { ReactLenis } from '@studio-freight/react-lenis';
import React from 'react';

// Wrap the library provider in a client component to avoid server component issues
export { ReactLenis };
